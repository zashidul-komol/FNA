<?php
	include('init.php');
	
	$action 		= new ProjectSetup();
	$opId 			= '';
	$userId 		= '';
	$role 			= '';
	$businessDate 	= '';
	if(isset($_SESSION['fmOperatorId'])) {
		$opId 			= $_SESSION['fmOperatorId'];
		$userId 		= $_SESSION['fmUserId'];
	}
	
	if(isset($opId) && (strlen($opId) > 0)) {
		$msg 				= '';
		
		
		
		// Insert UnLoad Opening Information Start
		if(isset($_POST['insertUnLoadOpeningInfo'])) {
			$unloadInsert 	= new projectSetupInsertNew();
			$msg 			= $unloadInsert->insertUnLoadOpeningInfo($userId);
		}
		// Insert UnLoad Opening Information End
		
		$body 				= $action->getUnLoadOpeningEntry($userId);
		if(strlen($msg)>0) {
			$body 			= str_replace('<!--%[MSG]%-->',$msg,$body);
		}
	} else {
		$body 				= $struc->getTemplateContent('login');
	}
	$struc->loadStructure($body);
?>